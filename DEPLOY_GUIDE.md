# GitHub Pages로 청첩장 올리고 도메인 연결하기 (초보자용)

전체 과정은 크게 3단계입니다.
1. GitHub에 청첩장 파일 올리기 → `아이디.github.io/저장소이름` 으로 접속 가능
2. (선택) 도메인 구매하기 → 예: `shinbeom-hana.com`
3. (선택) 산 도메인을 GitHub에 연결하기 → `shinbeom-hana.com` 으로 접속 가능

도메인 없이 1단계만 해도 청첩장 링크는 완성됩니다. 2, 3단계는 "더 예쁜 주소"를 갖고 싶을 때만 하면 됩니다.

---

## 1단계. GitHub 계정 만들기

1. https://github.com 접속
2. 오른쪽 위 **Sign up** 클릭
3. 이메일 / 비밀번호 / 아이디(username) 입력하고 안내에 따라 가입 완료
   - 이 아이디가 나중에 `아이디.github.io` 주소에 그대로 들어가니 신중하게 정하세요 (예: `shinbeom2026` 처럼)

이미 계정이 있다면 로그인만 하고 다음으로 넘어가세요.

---

## 2단계. 저장소(Repository) 만들기

"저장소"는 파일들을 담아두는 폴더라고 생각하면 됩니다.

1. GitHub 로그인 후 오른쪽 위 **+** 버튼 → **New repository** 클릭
2. **Repository name**에 원하는 이름 입력 (예: `wedding`, `our-wedding-2026` 등 영문/숫자/하이픈만 사용)
3. **Public** 선택되어 있는지 확인 (Private이면 무료 GitHub Pages가 안 켜질 수 있음)
4. 나머지는 그대로 두고 **Create repository** 클릭

---

## 3단계. 청첩장 파일 업로드하기

git 프로그램을 따로 설치할 필요 없이, 웹 브라우저에서 드래그 앤 드롭으로 끝낼 수 있습니다.

1. 방금 만든 저장소 페이지에서 **uploading an existing file** 링크 클릭
   (또는 상단의 **Add file** → **Upload files**)
2. 컴퓨터에서 압축 풀어둔 청첩장 폴더를 열고, **폴더 안의 내용물**을 전부 선택해서 끌어다 놓기
   - `index.html`, `style.css`, `script.js`, `images` 폴더, `README.md` 등
   - ⚠️ 폴더 자체를 통째로 끌어넣지 말고, **폴더 "안의" 파일/폴더들**을 넣어야 합니다.
     (index.html이 저장소의 맨 바깥에 바로 보여야 합니다)
3. 페이지 아래로 내려서 **Commit changes** 클릭 (커밋 메시지는 기본값 그대로 둬도 됩니다)
4. 업로드가 끝나면 저장소 페이지에 파일 목록이 보입니다. `index.html`이 맨 위 목록에 보이는지 확인하세요.

---

## 4단계. GitHub Pages 켜기

1. 저장소 페이지 상단 메뉴에서 **Settings** 클릭
2. 왼쪽 사이드바에서 **Pages** 클릭
3. **Build and deployment** 항목에서
   - Source: **Deploy from a branch** 선택
   - Branch: **main** 선택, 옆의 폴더는 **/(root)** 선택
   - **Save** 클릭
4. 몇 초~몇 분 기다리면 같은 페이지 위쪽에 초록색으로
   **"Your site is live at https://아이디.github.io/저장소이름/"** 라는 문구가 나타납니다.
5. 그 링크를 클릭하거나 새 탭에 직접 입력해서 청첩장이 잘 보이는지 확인하세요.

> 바로 안 뜨고 404 페이지가 나오면, 1~2분 더 기다렸다가 새로고침 해보세요. 그래도 안 되면 3단계에서 파일이 저장소 맨 바깥(루트)에 제대로 올라갔는지 다시 확인하세요.

이 링크가 바로 하객들에게 공유할 청첩장 주소입니다. 여기서 끝내도 됩니다.

---

## 5단계 (선택). 나만의 도메인 사서 연결하기

`아이디.github.io/저장소이름` 대신 `우리결혼식.com` 같은 짧고 예쁜 주소를 쓰고 싶다면 아래를 진행하세요.

### 5-1. 도메인 구매하기

도메인 등록업체 아무 곳이나 괜찮습니다. 한국어 지원이 편한 곳 예시:
- **가비아** (gabia.com)
- **후이즈** (whois.co.kr)
- 해외 서비스를 원하면 **Namecheap**, **Cloudflare Registrar** 등도 많이 씁니다.

원하는 도메인(예: `shinbeom-hana.com`)을 검색해서 구매하면 됩니다. `.com`은 보통 1년에 1~2만원대입니다.

### 5-2. GitHub에 커스텀 도메인 등록하기

1. 저장소 **Settings → Pages**로 이동
2. **Custom domain** 입력창에 구매한 도메인 입력
   - 예: `shinbeom-hana.com` (www 없이) 또는 `www.shinbeom-hana.com`
3. **Save** 클릭
   - 이때 저장소에 `CNAME`이라는 파일이 자동으로 하나 생깁니다. 지우지 마세요.

### 5-3. 도메인 등록업체에서 DNS 설정하기

구매한 도메인 관리 화면(가비아 기준 "DNS 관리" 또는 "네임서버 설정")에 들어가서
아래 표대로 레코드를 추가합니다. (등록업체마다 화면 용어가 조금씩 달라도 항목은 동일합니다)

**① 도메인 자체(예: shinbeom-hana.com)로 접속했을 때를 위한 A 레코드 4개**

| 타입 | 호스트/이름 | 값(주소) |
|---|---|---|
| A | @ (또는 비워둠) | 185.199.108.153 |
| A | @ (또는 비워둠) | 185.199.109.153 |
| A | @ (또는 비워둠) | 185.199.110.153 |
| A | @ (또는 비워둠) | 185.199.111.153 |

**② www로 접속했을 때를 위한 CNAME 레코드 1개**

| 타입 | 호스트/이름 | 값(주소) |
|---|---|---|
| CNAME | www | 아이디.github.io |

> 기존에 등록업체가 기본으로 만들어둔 A 레코드나 CNAME 레코드가 있다면(파킹 페이지 등) 먼저 삭제한 뒤 위 레코드를 추가하세요. 레코드가 겹치면 충돌이 납니다.

### 5-4. 확인하기

1. DNS 설정은 반영되는 데 **최대 24시간** 걸릴 수 있습니다 (보통 몇 분~몇 시간 내로 됩니다)
2. GitHub의 **Settings → Pages**로 돌아가서 Custom domain 아래 상태 확인
   - 정상 연결되면 "DNS check successful" 같은 초록색 체크 표시가 뜹니다
3. 연결이 확인되면 같은 화면에 **Enforce HTTPS** 체크박스가 활성화됩니다 → 체크해서 `https://`로 안전하게 접속되게 하세요
4. 브라우저에 도메인을 직접 입력해서 청첩장이 잘 뜨는지 확인

---

## 문제가 생겼을 때 체크리스트

- **404 Not Found**: `index.html`이 저장소 맨 바깥(루트)에 있는지, 폴더 통째로 안 올라갔는지 확인
- **CSS/이미지가 안 보임**: 파일/폴더 이름의 대소문자, 경로가 코드와 정확히 같은지 확인 (`images/gallery/photo1.jpg` 등)
- **도메인 연결이 안 됨**: DNS 반영 시간(최대 24시간) 기다리기, A 레코드 4개/CNAME이 정확히 입력됐는지, 기존 레코드와 충돌 없는지 확인
- **HTTPS 자물쇠가 안 뜸**: 도메인 연결이 "DNS check successful"로 뜬 뒤에 시간이 조금 더 지나야 인증서가 발급됩니다. 몇 시간 기다렸다가 Enforce HTTPS 체크

---

## 나중에 내용을 수정하고 싶을 때

파일을 고친 뒤 저장소 페이지에서 **Add file → Upload files**로 같은 파일명으로 다시 올리면
자동으로 덮어써지고, 저장 후 1분 정도면 실제 사이트에도 반영됩니다.
